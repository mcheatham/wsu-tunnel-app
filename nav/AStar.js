//Import the graph

/**
 * Class for finding paths using the A* algorithm
 */
class AStar {
    /**
     * Uses A* to find a path from one node to another
     * @param {number} start - The ID of the node to start at
     * @param {number} end - The ID of the node to end at
     * @return {Object} An object containing data representing the shortest path
     *  between two points
     */
    static getPath(start, end) {
      console.log('Attempting to find a path from ' + start + ' to ' + 'end');
    }
}
